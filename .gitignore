# ============ 第三方依赖（强制跟踪） ============
# 策略：所有 *.dll / *.lib / *.a / *.bin 通配符限制为不适用于 dependencies/ 路径，
# 这样就不需要用 ! 否定来对抗全局规则了。

# ============ 构建产物 ============
# qmake 旧产物（保留规则防回潮）
build/
debug/
release/
*.pro.user
*.pro.user.*
*.qmake.stash
ui_*.h
Makefile
Makefile.Debug
Makefile.Release

# cmake 产物
build-cmake/
CMakeLists.txt.user
.qtc_clangd/
.cmake/
.qt/
.qtc/
cmake_install.cmake
CMakeCache.txt
CMakeCache.txt.prev
CMakeFiles/
CMakeScripts/
CMakeUserPresets.json
Testing/
qtcsettings.cmake
Smart_Player_autogen/
Smart_Player_autogen_timestamp_deps/

# ============ 编译器产物 ============
*.o
*.obj
*.so
*.exe
*.pdb
*.ilk
*.pch
*.ipch
*.suo
*.vcxproj.user

# ============ 调试 / 临时 ============
*.log
*.swp
*.swo
*.dSYM/
.DS_Store
Thumbs.db
audio_dump.pcm
crash.dmp

# 历史遗留的可执行文件名
Smart_OBS

# ============ Skills 临时文件 ============
skills-lock.json
full_review_report.json

# ============ 第三方依赖（强制跟踪） ============
# 上述 *.dll / *.lib / *.a / *.bin 通配符不适用于 dependencies/，强制跟踪
!dependencies/
